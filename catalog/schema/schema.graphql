# import Product from "products.graphql"
# import Category from "categories.graphql"
# import InputProduct from "inputProduct.graphql"
# import Result from "result.graphql"

type Query {
    products: [Product]
    categories: [Category]
}

type Mutation {
    addProduct(product: InputProduct!): Result
    addCategory(category: InputCategory!): Result
    removeProduct(productId: ID!): Result
}